import { execSync } from 'child_process';
import path from 'path';

test.skip('CLI smoke test runs without error', () => {
  // This test is intentionally skipped because the CLI requires compilation.
});